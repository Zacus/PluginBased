{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug PluginBased (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "preLaunchTask": "CMake Build (Debug)",
            "sourceLanguages": ["cpp"]
        }
    ]
}
